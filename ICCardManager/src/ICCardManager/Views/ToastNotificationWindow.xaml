<Window x:Class="ICCardManager.Views.ToastNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="通知"
        MinWidth="{DynamicResource ToastMinWidth}"
        MaxWidth="{DynamicResource ToastMaxWidth}"
        MaxHeight="{DynamicResource ToastMaxHeight}"
        SizeToContent="WidthAndHeight"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        FontSize="{DynamicResource BaseFontSize}"
        AutomationProperties.Name="通知ウィンドウ"
        AutomationProperties.LiveSetting="Assertive">

    <Window.Resources>
        <!-- フェードイン・アウトアニメーション -->
        <Storyboard x:Key="FadeInAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="FadeOutAnimation">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>

    <Border x:Name="ToastBorder"
            CornerRadius="12"
            Margin="10"
            Padding="20,15"
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- アイコン -->
            <TextBlock x:Name="IconText"
                       Grid.Column="0"
                       FontSize="{DynamicResource IconFontSize}"
                       VerticalAlignment="Center"
                       Margin="0,0,15,0"
                       AutomationProperties.Name="通知アイコン"/>

            <!-- メッセージ -->
            <!-- Issue #1273: タイトルは単一行＋省略（CharacterEllipsis）でトースト高さ暴走を防止。
                 メッセージとサブメッセージは Wrap を維持しつつ Window.MaxHeight 内に収める。 -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="TitleText"
                           FontSize="{DynamicResource TitleFontSize}"
                           FontWeight="Bold"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis"
                           AutomationProperties.Name="通知タイトル"/>
                <TextBlock x:Name="MessageText"
                           FontSize="{DynamicResource BaseFontSize}"
                           Margin="0,5,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.8"
                           AutomationProperties.Name="通知メッセージ"/>
                <TextBlock x:Name="SubMessageText"
                           FontSize="{DynamicResource SmallFontSize}"
                           Margin="0,5,0,0"
                           TextWrapping="Wrap"
                           Opacity="0.6"
                           Visibility="Collapsed"
                           AutomationProperties.Name="追加メッセージ"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
