<Window x:Class="ICCardManager.Views.Dialogs.DataExportImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ICCardManager.ViewModels"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:DataExportImportViewModel}"
        Title="データエクスポート/インポート"
        Height="800"
        Width="800"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="600"
        MinWidth="700"
        FontSize="{DynamicResource BaseFontSize}"
        FocusManager.FocusedElement="{Binding ElementName=ExportDataTypeComboBox}"
        AutomationProperties.Name="データエクスポート/インポートダイアログ"
        AutomationProperties.HelpText="カード、職員、履歴データのCSVエクスポート・インポートができます。Escapeキーで閉じます。">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <vm:DataTypeToDisplayNameConverter x:Key="DataTypeConverter"/>
        <vm:ImportActionToDisplayNameConverter x:Key="ImportActionConverter"/>
        <!-- Issue #511: 追加のコンバーター -->
        <common:InverseBooleanConverter x:Key="InverseBoolConverter" xmlns:common="clr-namespace:ICCardManager.Common"/>
        <common:BoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" xmlns:common="clr-namespace:ICCardManager.Common"/>
        <!-- Issue #638: 文字列/整数値用Visibilityコンバーター -->
        <common:StringToVisibilityConverter x:Key="StringToVisibilityConverter" xmlns:common="clr-namespace:ICCardManager.Common"/>
        <common:IntToVisibilityConverter x:Key="IntToVisibilityConverter" xmlns:common="clr-namespace:ICCardManager.Common"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Row 0: エクスポート設定 -->
            <RowDefinition Height="*"/>      <!-- Row 1: インポート設定（スクロール可能、残領域を使用） -->
            <RowDefinition Height="Auto"/>   <!-- Row 2: アクションボタン -->
            <RowDefinition Height="Auto"/>   <!-- Row 3: ステータスメッセージ -->
            <RowDefinition Height="2*" MinHeight="0"/>  <!-- Row 4: プレビュー結果＋エラー（リサイズ可能） -->
            <RowDefinition Height="Auto"/>   <!-- Row 5: Close button（常に表示） -->
        </Grid.RowDefinitions>

        <!-- エクスポート設定 -->
        <GroupBox Grid.Row="0" Header="エクスポート" Margin="0,0,0,10" Padding="15">
            <StackPanel>
                <TextBlock Text="データをCSVファイルに出力します"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource SmallFontSize}"
                           Margin="0,0,0,10"/>

                <!-- データタイプ選択 -->
                <!-- Issue #1276: TextBlock → Label + Target でアクセスキーとスクリーンリーダー連携を実装 -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="データ種別(_T):"
                           Target="{Binding ElementName=ExportDataTypeComboBox}"
                           VerticalAlignment="Center"
                           Padding="0"
                           FontSize="{DynamicResource BaseFontSize}"/>
                    <ComboBox x:Name="ExportDataTypeComboBox"
                              Grid.Column="1"
                              ItemsSource="{Binding ExportDataTypes}"
                              SelectedItem="{Binding SelectedExportType}"
                              Padding="8"
                              HorizontalAlignment="Left"
                              Width="200"
                              AutomationProperties.Name="エクスポートするデータ種別"
                              ToolTip="エクスポートするデータの種類を選択します（アクセスキー: Alt+T）">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource DataTypeConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>

                <!-- 日付範囲（履歴の場合のみ表示） -->
                <Grid Margin="0,0,0,10"
                      Visibility="{Binding IsLedgerExportSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="対象期間(_S):"
                           Target="{Binding ElementName=ExportStartDatePicker}"
                           VerticalAlignment="Center"
                           Padding="0"
                           FontSize="{DynamicResource BaseFontSize}"/>
                    <DatePicker x:Name="ExportStartDatePicker"
                                Grid.Column="1"
                                SelectedDate="{Binding ExportStartDate}"
                                Padding="5"
                                Width="130"
                                AutomationProperties.Name="開始日"
                                ToolTip="エクスポート対象期間の開始日（アクセスキー: Alt+S）"/>
                    <TextBlock Grid.Column="2"
                               Text=" ～ "
                               VerticalAlignment="Center"
                               Margin="5,0"/>
                    <DatePicker x:Name="ExportEndDatePicker"
                                Grid.Column="3"
                                SelectedDate="{Binding ExportEndDate}"
                                Padding="5"
                                Width="130"
                                AutomationProperties.Name="終了日"
                                ToolTip="エクスポート対象期間の終了日"/>
                </Grid>

                <!-- 削除済みを含む -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Grid.Column="1"
                              Content="削除済みデータを含める(_D)"
                              IsChecked="{Binding IncludeDeletedInExport}"
                              VerticalAlignment="Center"
                              AutomationProperties.Name="削除済みデータを含める"
                              ToolTip="論理削除されたデータもエクスポートに含めます（アクセスキー: Alt+D）"/>
                </Grid>

                <!-- エクスポートボタン -->
                <Button Content="CSVエクスポート(_X)"
                        Command="{Binding ExportCommand}"
                        Padding="15,10"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource SuccessActionBrush}"
                        Foreground="{DynamicResource OnPrimaryBrush}"
                        AutomationProperties.Name="CSVエクスポート実行"
                        ToolTip="選択したデータをCSVファイルにエクスポートします（アクセスキー: Alt+X）"/>

                <!-- 最後にエクスポートしたファイル -->
                <StackPanel Orientation="Horizontal"
                            Margin="0,10,0,0"
                            Visibility="{Binding LastExportedFile, Converter={StaticResource StringToVisibilityConverter}}">
                    <TextBlock Text="出力先: "
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="{DynamicResource SmallFontSize}"
                               VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding LastExportedFile}"
                               Foreground="{DynamicResource SecondaryTextBrush}"
                               FontSize="{DynamicResource SmallFontSize}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               MaxWidth="300"/>
                    <Button Content="開く"
                            Command="{Binding OpenExportedFileCommand}"
                            Padding="5,2"
                            Margin="10,0,0,0"
                            FontSize="{DynamicResource SmallFontSize}"/>
                    <Button Content="フォルダを開く"
                            Command="{Binding OpenExportFolderCommand}"
                            Padding="5,2"
                            Margin="5,0,0,0"
                            FontSize="{DynamicResource SmallFontSize}"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <!-- インポート設定（スクロール可能エリア） -->
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,10">
        <GroupBox Header="インポート" Padding="15">
            <StackPanel>
                <TextBlock Text="CSVファイルからデータを取り込みます"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource SmallFontSize}"
                           Margin="0,0,0,10"/>

                <!-- データタイプ選択 -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="データ種別(_I):"
                           Target="{Binding ElementName=ImportDataTypeComboBox}"
                           VerticalAlignment="Center"
                           Padding="0"
                           FontSize="{DynamicResource BaseFontSize}"/>
                    <ComboBox x:Name="ImportDataTypeComboBox"
                              Grid.Column="1"
                              ItemsSource="{Binding ImportDataTypes}"
                              SelectedItem="{Binding SelectedImportType}"
                              Padding="8"
                              HorizontalAlignment="Left"
                              Width="200"
                              AutomationProperties.Name="インポートするデータ種別"
                              ToolTip="インポートするデータの種類を選択します（アクセスキー: Alt+I）">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource DataTypeConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Grid>

                <!-- 既存スキップ -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <CheckBox Grid.Column="1"
                              Content="既存データはスキップする(_K)"
                              IsChecked="{Binding SkipExistingOnImport}"
                              VerticalAlignment="Center"
                              AutomationProperties.Name="既存データをスキップ"
                              ToolTip="同じIDmのデータが既に存在する場合はスキップします（オフの場合は上書き更新）（アクセスキー: Alt+K）"/>
                </Grid>

                <!-- Issue #511: カード指定セクション（利用履歴インポート時のみ表示） -->
                <Border BorderBrush="{DynamicResource NeutralBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="10"
                        Margin="0,0,0,10"
                        Background="{DynamicResource WaitingBackgroundBrush}"
                        Visibility="{Binding IsLedgerImportSelected, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="インポート先カード"
                                   FontWeight="Bold"
                                   FontSize="{DynamicResource BaseFontSize}"
                                   Margin="0,0,0,5"/>
                        <TextBlock Text="CSVにカードIDmが含まれない場合、ここで指定したカードの履歴としてインポートします"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontSize="{DynamicResource SmallFontSize}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,10"/>

                        <!-- 方法1: 一覧から選択 -->
                        <RadioButton Content="登録済みカード一覧から選択"
                                     IsChecked="{Binding UseCardListSelection}"
                                     GroupName="CardSelectionMethod"
                                     Margin="0,0,0,5"
                                     AutomationProperties.Name="登録済みカード一覧から選択"
                                     ToolTip="既に登録されているカードの一覧から選択します"/>
                        <ComboBox ItemsSource="{Binding AvailableCards}"
                                  SelectedItem="{Binding SelectedTargetCard}"
                                  IsEnabled="{Binding UseCardListSelection}"
                                  Padding="8"
                                  Margin="20,0,0,10"
                                  HorizontalAlignment="Left"
                                  Width="350"
                                  AutomationProperties.Name="インポート先カード選択"
                                  ToolTip="インポート先のカードを選択します">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding CardType}" FontWeight="SemiBold"/>
                                        <TextBlock Text=" " />
                                        <TextBlock Text="{Binding CardNumber}"/>
                                        <TextBlock Text=" (" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                        <TextBlock Text="{Binding CardIdm}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="{DynamicResource SmallFontSize}"/>
                                        <TextBlock Text=")" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <!-- 方法2: カードタッチ -->
                        <RadioButton Content="カードをタッチして指定"
                                     IsChecked="{Binding UseCardListSelection, Converter={StaticResource InverseBoolConverter}}"
                                     IsEnabled="{Binding IsCardReaderAvailable}"
                                     GroupName="CardSelectionMethod"
                                     Margin="0,0,0,5"
                                     AutomationProperties.Name="カードをタッチして指定"
                                     ToolTip="交通系ICカードをリーダーにタッチしてカードを指定します">
                            <RadioButton.Style>
                                <Style TargetType="RadioButton">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCardReaderAvailable}" Value="False">
                                            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </RadioButton.Style>
                        </RadioButton>
                        <StackPanel Orientation="Horizontal" Margin="20,0,0,5">
                            <Button Content="カードをタッチ"
                                    Command="{Binding StartCardTouchCommand}"
                                    Padding="10,5"
                                    Background="{DynamicResource HeaderBackgroundBrush}"
                                    Foreground="{DynamicResource OnPrimaryBrush}"
                                    AutomationProperties.Name="カードタッチ開始"
                                    ToolTip="カードリーダーでカードを読み取ります">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UseCardListSelection}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsCardReaderAvailable}" Value="False">
                                                <Setter Property="IsEnabled" Value="False"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="キャンセル"
                                    Command="{Binding CancelCardTouchCommand}"
                                    Padding="10,5"
                                    Margin="5,0,0,0"
                                    Visibility="{Binding IsWaitingForCardTouch, Converter={StaticResource BoolToVisibilityConverter}}"
                                    AutomationProperties.Name="カードタッチキャンセル"/>
                            <TextBlock Text="{Binding TouchedCardInfo}"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       FontSize="{DynamicResource BaseFontSize}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource InfoTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding TouchedCardInfo}" Value="未登録のカードです">
                                                <Setter Property="Foreground" Value="{DynamicResource DangerTextBrush}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding TouchedCardInfo}" Value="カードをタッチしてください...">
                                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                                <Setter Property="FontStyle" Value="Italic"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <!-- カードリーダー未接続の場合の注意 -->
                        <TextBlock Text="※ カードリーダーが接続されていません"
                                   Foreground="{DynamicResource DangerTextBrush}"
                                   FontSize="{DynamicResource SmallFontSize}"
                                   Margin="20,5,0,0"
                                   Visibility="{Binding IsCardReaderAvailable, Converter={StaticResource InverseBoolToVisibilityConverter}, ConverterParameter=invert}"/>

                        <!-- クリアボタン -->
                        <Button Content="指定をクリア"
                                Command="{Binding ClearTargetCardCommand}"
                                Padding="8,4"
                                Margin="0,10,0,0"
                                HorizontalAlignment="Left"
                                FontSize="{DynamicResource SmallFontSize}"
                                AutomationProperties.Name="カード指定をクリア"
                                ToolTip="選択したカードの指定を解除します"/>
                    </StackPanel>
                </Border>

                <!-- 最後にインポートしたファイル -->
                <TextBlock Text="{Binding LastImportedFile, StringFormat=インポート完了: {0}}"
                           Foreground="{DynamicResource SecondaryTextBrush}"
                           FontSize="{DynamicResource SmallFontSize}"
                           TextTrimming="CharacterEllipsis"
                           Visibility="{Binding LastImportedFile, Converter={StaticResource StringToVisibilityConverter}}"/>
            </StackPanel>
        </GroupBox>
        </ScrollViewer>

        <!-- アクションボタン（常に表示） -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="プレビュー(_P)"
                    Command="{Binding PreviewImportCommand}"
                    Padding="15,10"
                    Background="{DynamicResource WarningActionBrush}"
                    Foreground="{DynamicResource OnPrimaryBrush}"
                    AutomationProperties.Name="インポートプレビュー"
                    ToolTip="CSVファイルの内容をプレビューし、変更点を確認します（アクセスキー: Alt+P）"/>
            <Button Content="インポート実行(_M)"
                    Command="{Binding ExecuteImportCommand}"
                    Padding="15,10"
                    Margin="10,0,0,0"
                    Background="{DynamicResource SuccessActionBrush}"
                    Foreground="{DynamicResource OnPrimaryBrush}"
                    IsEnabled="{Binding HasPreview}"
                    AutomationProperties.Name="インポート実行"
                    ToolTip="プレビュー済みのデータをインポートします（アクセスキー: Alt+M）"/>
            <Button Content="直接インポート(_R)"
                    Command="{Binding ImportCommand}"
                    Padding="15,10"
                    Margin="10,0,0,0"
                    Background="{DynamicResource HeaderBackgroundBrush}"
                    Foreground="{DynamicResource OnPrimaryBrush}"
                    AutomationProperties.Name="直接CSVインポート"
                    ToolTip="プレビューなしで直接インポートします（アクセスキー: Alt+R）"/>
        </StackPanel>

        <!-- ステータスメッセージ -->
        <Border Grid.Row="3"
                Padding="10"
                Margin="0,0,0,10"
                CornerRadius="5"
                Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{DynamicResource ReturnBackgroundBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ErrorBackgroundBrush}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="{DynamicResource BaseFontSize}"
                       TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="{DynamicResource InfoTextBrush}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource DangerTextBrush}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>

        <!-- Issue #1063: プレビュー結果＋エラー一覧 -->
        <!-- エラー行をAutoにし、エラーがない時はプレビューが全領域を使用 -->
        <Grid Grid.Row="4" Margin="0,5,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>     <!-- プレビュー結果（残り全領域を使用） -->
                <RowDefinition Height="Auto"/>  <!-- エラー一覧（内容に応じてサイズ変動） -->
            </Grid.RowDefinitions>

            <!-- プレビュー結果セクション -->
            <Grid Grid.Row="0"
                  Visibility="{Binding HasPreview, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>  <!-- サマリーバナー -->
                    <RowDefinition Height="*"/>     <!-- DataGrid -->
                </Grid.RowDefinitions>

                <!-- プレビューサマリー -->
                <Border Grid.Row="0" Background="{DynamicResource LendingBackgroundBrush}" Padding="10" CornerRadius="3" Margin="0,0,0,5">
                    <StackPanel>
                        <TextBlock FontWeight="Bold" FontSize="{DynamicResource BaseFontSize}">
                            <Run Text="プレビュー結果: "/>
                            <Run Text="{Binding PreviewSummary}" Foreground="{DynamicResource LendingForegroundBrush}"/>
                        </TextBlock>
                        <TextBlock Text="{Binding ImportPreviewFile, StringFormat=ファイル: {0}}"
                                   Foreground="{DynamicResource SecondaryTextBrush}"
                                   FontSize="{DynamicResource SmallFontSize}"
                                   Margin="0,5,0,0"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>

                <!-- プレビューDataGrid -->
                <!-- Issue #1063: CanContentScroll=Falseでピクセル単位スクロールに切替え、
                     RowDetails展開時に最下行の内容が切れないようにする -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding PreviewItems}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="{DynamicResource SubtleBorderBrush}"
                          ScrollViewer.CanContentScroll="False"
                          AutomationProperties.Name="インポートプレビュー一覧">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="行" Binding="{Binding LineNumber}" Width="50"/>
                        <DataGridTextColumn Binding="{Binding Idm}" Width="220">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DataContext.PreviewColumn1Header, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding Name}" Width="120">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DataContext.PreviewColumn2Header, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTextColumn Binding="{Binding AdditionalInfo}" Width="100">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DataContext.PreviewColumn3Header, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                </DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="アクション" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Action, Converter={StaticResource ImportActionConverter}}"
                                               Padding="5,2">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Action}" Value="Insert">
                                                        <Setter Property="Foreground" Value="{DynamicResource SuccessActionBrush}"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Update">
                                                        <Setter Property="Foreground" Value="{DynamicResource WarningActionBrush}"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Skip">
                                                        <Setter Property="Foreground" Value="{DynamicResource WaitingBorderBrush}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Action}" Value="Restore">
                                                        <Setter Property="Foreground" Value="{DynamicResource HeaderBackgroundBrush}"/>
                                                        <Setter Property="FontWeight" Value="Bold"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="変更点" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ChangesSummary}"
                                               Foreground="{DynamicResource LendingForegroundBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding ChangesSummary}"
                                               Padding="5,2">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasChanges}" Value="True">
                                                        <Setter Property="FontStyle" Value="Italic"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource WarningBackgroundBrush}" Padding="10" Margin="0,2"
                                    Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="{Binding ChangesHeader}" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <ItemsControl ItemsSource="{Binding Changes}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding DisplayText}"
                                                           Foreground="#795548"
                                                           Margin="10,2,0,2"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </Grid>

            <!-- エラー一覧セクション -->
            <Border Grid.Row="1" Background="{DynamicResource LendingBackgroundBrush}" Padding="10" CornerRadius="3"
                    MaxHeight="200"
                    Visibility="{Binding ImportErrors.Count, Converter={StaticResource IntToVisibilityConverter}}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="エラー詳細" FontWeight="Bold" FontSize="{DynamicResource BaseFontSize}" Margin="0,0,0,5"/>
                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding ImportErrors}"
                             Background="{DynamicResource LendingBackgroundBrush}"
                             BorderThickness="0"
                             AutomationProperties.Name="インポートエラー一覧">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}"
                                           Foreground="{DynamicResource LendingForegroundBrush}"
                                           FontSize="{DynamicResource SmallFontSize}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- 閉じるボタン（常に画面下部に表示） -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="閉じる(_C)"
                    Click="CloseButton_Click"
                    Padding="20,10"
                    MinWidth="100"
                    IsCancel="True"
                    AutomationProperties.Name="ダイアログを閉じる"
                    ToolTip="ダイアログを閉じます (Escape / Alt+C)"/>
        </StackPanel>

        <!-- 処理中オーバーレイ -->
        <Border Grid.RowSpan="6"
                Background="{DynamicResource OverlayBrush}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="{Binding IsIndeterminate}"
                             Value="{Binding ProgressValue}"
                             Maximum="{Binding ProgressMax}"
                             Width="200" Height="20"
                             Foreground="{DynamicResource PrimaryBrush}"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="{DynamicResource OnPrimaryBrush}"
                           FontSize="{DynamicResource BaseFontSize}"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
