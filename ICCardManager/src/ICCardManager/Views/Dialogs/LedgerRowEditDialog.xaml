<Window x:Class="ICCardManager.Views.Dialogs.LedgerRowEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ICCardManager.ViewModels"
        xmlns:validation="clr-namespace:ICCardManager.Common.Validation"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:LedgerRowEditViewModel}"
        Title="{Binding DialogTitle}"
        Height="620"
        Width="700"
        MinHeight="400"
        MinWidth="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FontSize="{DynamicResource BaseFontSize}"
        FocusManager.FocusedElement="{Binding ElementName=EditDatePicker}"
        AutomationProperties.Name="履歴行の追加・修正ダイアログ"
        AutomationProperties.HelpText="履歴の行を追加または全項目を修正できます。">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <common:StringToVisibilityConverter x:Key="StringToVisibilityConverter" xmlns:common="clr-namespace:ICCardManager.Common"/>
        <conv:InverseBoolConverter x:Key="InvertBoolConverter"
                                    xmlns:conv="clr-namespace:ICCardManager.Views.Converters"/>
    </Window.Resources>

    <!-- Issue #1280: 下部ボタンエリアを画面底部に固定し、上部の入力フォームのみ縮小可能にするため
         Row 1 を * にし、フォーム部分を ScrollViewer で囲む。これにより低解像度環境でも
         キャンセル/保存ボタンが常にアクセス可能になる。 -->
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- パンくずナビゲーション（Issue #1134） -->
        <TextBlock Grid.Row="0"
                   Text="{Binding BreadcrumbText}"
                   FontSize="{DynamicResource SmallFontSize}"
                   Foreground="{DynamicResource SecondaryTextBrush}"
                   Margin="0,0,0,6"
                   Visibility="{Binding BreadcrumbText, Converter={StaticResource StringToVisibilityConverter}}"
                   AutomationProperties.Name="パンくずナビゲーション"/>

        <!-- 入力フィールド -->
        <!-- Issue #1280: ScrollViewer で囲み、低解像度でフォームが縦方向にスクロール可能にする -->
        <ScrollViewer Grid.Row="1" Margin="0,0,0,10"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Issue #1276: TextBlock → Label + Target でアクセスキーとスクリーンリーダー連携を実装 -->
            <!-- 日付 (_D) -->
            <Label Grid.Row="0" Grid.Column="0"
                   Content="日付(_D):"
                   Target="{Binding ElementName=EditDatePicker}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <DatePicker x:Name="EditDatePicker"
                        Grid.Row="0" Grid.Column="1"
                        SelectedDate="{Binding EditDate}"
                        FontSize="{DynamicResource BaseFontSize}"
                        Margin="0,0,0,8"
                        AutomationProperties.Name="出納日付"
                        ToolTip="出納年月日を選択してください（アクセスキー: Alt+D）"/>

            <!-- 摘要 (_S) -->
            <Label Grid.Row="1" Grid.Column="0"
                   Content="摘要(_S):"
                   Target="{Binding ElementName=SummaryTextBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <TextBox x:Name="SummaryTextBox"
                     Grid.Row="1" Grid.Column="1"
                     Text="{Binding Summary, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="{DynamicResource BaseFontSize}"
                     Padding="10,6"
                     Margin="0,0,0,8"
                     AutomationProperties.Name="摘要"
                     ToolTip="摘要（例: 鉄道（博多駅～天神）、役務費によりチャージ）（アクセスキー: Alt+S）"/>

            <!-- 受入金額 (_I) -->
            <Label Grid.Row="2" Grid.Column="0"
                   Content="受入(_I):"
                   Target="{Binding ElementName=IncomeTextBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                <!-- Issue #1279: 負数を入力エラーとして視覚的に表示 -->
                <TextBox x:Name="IncomeTextBox"
                         FontSize="{DynamicResource BaseFontSize}"
                         Padding="10,6"
                         Width="120"
                         AutomationProperties.Name="受入金額"
                         ToolTip="チャージ額（受入金額）（アクセスキー: Alt+I）">
                    <TextBox.Text>
                        <Binding Path="Income" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validation:NumericRangeValidationRule
                                    Min="0"
                                    FieldName="受入金額"/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <TextBlock Text="円" VerticalAlignment="Center" Margin="5,0,0,0"
                           FontSize="{DynamicResource BaseFontSize}"/>
            </StackPanel>

            <!-- 払出金額 (_E) -->
            <Label Grid.Row="3" Grid.Column="0"
                   Content="払出(_E):"
                   Target="{Binding ElementName=ExpenseTextBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox x:Name="ExpenseTextBox"
                         FontSize="{DynamicResource BaseFontSize}"
                         Padding="10,6"
                         Width="120"
                         AutomationProperties.Name="払出金額"
                         ToolTip="利用額（払出金額）（アクセスキー: Alt+E）">
                    <TextBox.Text>
                        <Binding Path="Expense" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validation:NumericRangeValidationRule
                                    Min="0"
                                    FieldName="払出金額"/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <TextBlock Text="円" VerticalAlignment="Center" Margin="5,0,0,0"
                           FontSize="{DynamicResource BaseFontSize}"/>
            </StackPanel>

            <!-- 残高 (_B) -->
            <Label Grid.Row="4" Grid.Column="0"
                   Content="残高(_B):"
                   Target="{Binding ElementName=BalanceTextBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox x:Name="BalanceTextBox"
                         Text="{Binding Balance, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="{DynamicResource BaseFontSize}"
                         Padding="10,6"
                         Width="120"
                         IsEnabled="{Binding IsAutoBalance, Converter={StaticResource InvertBoolConverter}}"
                         AutomationProperties.Name="残高"
                         ToolTip="残高（自動計算ONの場合は自動で計算されます）（アクセスキー: Alt+B）"/>
                <TextBlock Text="円" VerticalAlignment="Center" Margin="5,0,0,0"
                           FontSize="{DynamicResource BaseFontSize}"/>
                <CheckBox Content="自動計算(_A)"
                          IsChecked="{Binding IsAutoBalance}"
                          VerticalAlignment="Center"
                          Margin="15,0,0,0"
                          FontSize="{DynamicResource BaseFontSize}"
                          AutomationProperties.Name="残高を自動計算"
                          ToolTip="ONにすると、前行の残高 + 受入 - 払出 で自動計算されます（アクセスキー: Alt+A）"/>
            </StackPanel>

            <!-- 利用者 (_U) -->
            <Label Grid.Row="5" Grid.Column="0"
                   Content="利用者(_U):"
                   Target="{Binding ElementName=StaffComboBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Center"
                   Padding="0"
                   Margin="0,0,0,8"/>
            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,8">
                <ComboBox x:Name="StaffComboBox"
                          ItemsSource="{Binding StaffList}"
                          SelectedItem="{Binding SelectedStaff}"
                          DisplayMemberPath="Name"
                          FontSize="{DynamicResource BaseFontSize}"
                          MinWidth="150"
                          AutomationProperties.Name="利用者を選択"
                          ToolTip="利用者を選択（任意）（アクセスキー: Alt+U）"/>
                <Button Content="×"
                        Command="{Binding ClearStaffCommand}"
                        FontSize="{DynamicResource BaseFontSize}"
                        Padding="6,1"
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        AutomationProperties.Name="利用者をクリア"
                        ToolTip="利用者の選択を解除して空欄にします"/>
            </StackPanel>

            <!-- 備考 (_N) -->
            <Label Grid.Row="6" Grid.Column="0"
                   Content="備考(_N):"
                   Target="{Binding ElementName=NoteTextBox}"
                   FontSize="{DynamicResource BaseFontSize}"
                   VerticalAlignment="Top"
                   Padding="0"
                   Margin="0,6,0,0"/>
            <TextBox x:Name="NoteTextBox"
                     Grid.Row="6" Grid.Column="1"
                     Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}"
                     FontSize="{DynamicResource BaseFontSize}"
                     Padding="10,6"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     MinHeight="50"
                     AutomationProperties.Name="備考"
                     ToolTip="備考（任意）（アクセスキー: Alt+N）"/>
        </Grid>
        </ScrollViewer>

        <!-- 挿入位置プレビュー（Addモードのみ） -->
        <Border Grid.Row="2"
                Background="{DynamicResource NeutralBackgroundBrush}"
                CornerRadius="4"
                Padding="10"
                Margin="0,5,0,5"
                Visibility="{Binding IsAddMode, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="■ 挿入位置プレビュー"
                           FontWeight="Bold"
                           FontSize="{DynamicResource BaseFontSize}"
                           Margin="0,0,0,5"/>

                <ItemsControl x:Name="InsertPreviewList" ItemsSource="{Binding ContextRows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock FontSize="{DynamicResource SmallFontSize}"
                                       FontFamily="Consolas, MS Gothic"
                                       Margin="5,1">
                                <Run Text="{Binding DateDisplay, Mode=OneWay}"/>
                                <Run Text="  "/>
                                <Run Text="{Binding Summary, Mode=OneWay}"/>
                                <Run Text="  "/>
                                <Run Text="{Binding BalanceDisplay, Mode=OneWay}"/>
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 挿入位置マーカー -->
                <Border Background="{DynamicResource LendingBackgroundBrush}" CornerRadius="4" Padding="8,4" Margin="0,3">
                    <TextBlock Text="▶▶▶ ここに挿入 ◀◀◀"
                               Foreground="{DynamicResource LendingForegroundBrush}"
                               FontWeight="Bold"
                               FontSize="{DynamicResource BaseFontSize}"
                               HorizontalAlignment="Center"/>
                </Border>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <Button Content="▲ 上へ"
                            Command="{Binding MoveInsertPositionUpCommand}"
                            Padding="12,4"
                            Margin="0,0,10,0"
                            AutomationProperties.Name="挿入位置を上へ"
                            ToolTip="挿入位置を1つ上に移動"/>
                    <Button Content="▼ 下へ"
                            Command="{Binding MoveInsertPositionDownCommand}"
                            Padding="12,4"
                            AutomationProperties.Name="挿入位置を下へ"
                            ToolTip="挿入位置を1つ下に移動"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 警告表示エリア -->
        <StackPanel Grid.Row="3" Margin="0,5,0,5">
            <!-- バリデーションエラー -->
            <TextBlock Text="{Binding ValidationMessage}"
                       Foreground="{DynamicResource DangerTextBrush}"
                       FontSize="{DynamicResource SmallFontSize}"
                       FontWeight="Bold">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ValidationMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- 警告メッセージ -->
            <Border Background="{DynamicResource LendingBackgroundBrush}"
                    CornerRadius="4"
                    Padding="10,5"
                    Margin="0,3,0,0">
                <TextBlock Text="{Binding WarningMessage}"
                           Foreground="{DynamicResource LendingForegroundBrush}"
                           FontSize="{DynamicResource SmallFontSize}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding WarningMessage}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding WarningMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
            </Border>

            <!-- ステータスメッセージ -->
            <TextBlock Text="{Binding StatusMessage}"
                       Foreground="{DynamicResource DangerTextBrush}"
                       FontSize="{DynamicResource SmallFontSize}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- ボタン -->
        <Grid Grid.Row="4" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Issue #750: 削除ボタン（左下に配置し、保存/キャンセルと離す） -->
            <!-- Issue #1276: アクセスキー _X -->
            <Button Grid.Column="0"
                    Content="削除(_X)"
                    Command="{Binding RequestDeleteCommand}"
                    Visibility="{Binding CanDelete, Converter={StaticResource BoolToVisibilityConverter}}"
                    Padding="20,10"
                    Foreground="{DynamicResource DangerTextBrush}"
                    FontSize="{DynamicResource BaseFontSize}"
                    AutomationProperties.Name="この履歴を削除"
                    ToolTip="この履歴を削除します（アクセスキー: Alt+X）"/>

            <!-- 保存/キャンセルボタン（右下） -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal">
                <Button Content="キャンセル(_C)"
                        Command="{Binding CancelCommand}"
                        Click="CancelButton_Click"
                        Padding="20,10"
                        Margin="0,0,10,0"
                        IsCancel="True"
                        FontSize="{DynamicResource BaseFontSize}"
                        AutomationProperties.Name="キャンセル"
                        ToolTip="変更を破棄してダイアログを閉じる (Escape / Alt+C)"/>
                <!-- 戻る（Issue #1134） -->
                <Button Content="◀ 戻る"
                        Command="{Binding BackCommand}"
                        Visibility="{Binding ShowSaveAndNextButton, Converter={StaticResource BoolToVisibilityConverter}}"
                        Padding="16,10"
                        Margin="0,0,6,0"
                        FontSize="{DynamicResource BaseFontSize}"
                        AutomationProperties.Name="前の履歴へ戻る"
                        ToolTip="前の履歴を開きます"/>
                <!-- 次へ（保存しない）（Issue #1134） -->
                <Button Content="次へ ▶"
                        Command="{Binding SkipToNextCommand}"
                        Visibility="{Binding ShowSaveAndNextButton, Converter={StaticResource BoolToVisibilityConverter}}"
                        Padding="16,10"
                        Margin="0,0,6,0"
                        FontSize="{DynamicResource BaseFontSize}"
                        AutomationProperties.Name="保存せず次の履歴へ"
                        ToolTip="保存せずに次の履歴を開きます"/>
                <!-- 保存して次へ（Issue #1134） -->
                <Button Content="保存して次へ"
                        Command="{Binding SaveAndEditNextCommand}"
                        IsEnabled="{Binding CanSave}"
                        Visibility="{Binding ShowSaveAndNextButton, Converter={StaticResource BoolToVisibilityConverter}}"
                        Padding="20,10"
                        Margin="0,0,10,0"
                        Background="{DynamicResource InfoTextBrush}"
                        Foreground="{DynamicResource OnPrimaryBrush}"
                        FontSize="{DynamicResource BaseFontSize}"
                        AutomationProperties.Name="保存して次の履歴を編集"
                        ToolTip="保存して次の履歴を開きます"/>
                <Button Content="保存"
                        Command="{Binding SaveCommand}"
                        IsEnabled="{Binding CanSave}"
                        Padding="25,10"
                        Background="{DynamicResource SuccessActionBrush}"
                        Foreground="{DynamicResource OnPrimaryBrush}"
                        IsDefault="True"
                        FontSize="{DynamicResource BaseFontSize}"
                        AutomationProperties.Name="保存"
                        ToolTip="保存 (Enter)"/>
            </StackPanel>
        </Grid>

        <!-- 処理中オーバーレイ -->
        <Border Grid.RowSpan="6"
                Background="{DynamicResource OverlayBrush}"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="{DynamicResource OnPrimaryBrush}"
                           FontSize="{DynamicResource BaseFontSize}"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
