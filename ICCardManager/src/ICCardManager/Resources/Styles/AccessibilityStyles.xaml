<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- =====================================
         アクセシビリティ対応スタイル
         WCAG 2.1 AA準拠
         ===================================== -->

    <!-- ===== 色定義（WCAG 2.1 AA準拠） ===== -->
    <!-- コントラスト比4.5:1以上を確保 -->

    <!-- 貸出状態（暖色系） - オレンジ/アンバー -->
    <SolidColorBrush x:Key="LendingBackgroundBrush" Color="#FFF3E0"/>
    <SolidColorBrush x:Key="LendingForegroundBrush" Color="#E65100"/>
    <SolidColorBrush x:Key="LendingBorderBrush" Color="#FF9800"/>

    <!-- 返却状態（寒色系） - 青/シアン -->
    <SolidColorBrush x:Key="ReturnBackgroundBrush" Color="#E3F2FD"/>
    <SolidColorBrush x:Key="ReturnForegroundBrush" Color="#0D47A1"/>
    <SolidColorBrush x:Key="ReturnBorderBrush" Color="#2196F3"/>

    <!-- エラー状態 - 赤 -->
    <SolidColorBrush x:Key="ErrorBackgroundBrush" Color="#FFEBEE"/>
    <SolidColorBrush x:Key="ErrorForegroundBrush" Color="#B71C1C"/>
    <SolidColorBrush x:Key="ErrorBorderBrush" Color="#F44336"/>

    <!-- 成功状態 - 緑 -->
    <SolidColorBrush x:Key="SuccessBackgroundBrush" Color="#E8F5E9"/>
    <SolidColorBrush x:Key="SuccessForegroundBrush" Color="#1B5E20"/>
    <SolidColorBrush x:Key="SuccessBorderBrush" Color="#4CAF50"/>

    <!-- 警告状態 - 黄色 -->
    <SolidColorBrush x:Key="WarningBackgroundBrush" Color="#FFFDE7"/>
    <SolidColorBrush x:Key="WarningForegroundBrush" Color="#F57F17"/>
    <SolidColorBrush x:Key="WarningBorderBrush" Color="#FFC107"/>

    <!-- 待機状態 - グレー -->
    <SolidColorBrush x:Key="WaitingBackgroundBrush" Color="#FAFAFA"/>
    <SolidColorBrush x:Key="WaitingForegroundBrush" Color="#424242"/>
    <SolidColorBrush x:Key="WaitingBorderBrush" Color="#9E9E9E"/>

    <!-- プライマリカラー -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="#1976D2"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#0D47A1"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="#BBDEFB"/>

    <!-- 統合選択状態（チェック済み行） -->
    <SolidColorBrush x:Key="CheckedRowBackgroundBrush" Color="#BBDEFB"/>

    <!-- ===== アプリケーション共通カラー（Issue #721） ===== -->

    <!-- ヘッダー・アクセントカラー -->
    <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#2196F3"/>
    <SolidColorBrush x:Key="InfoTextBrush" Color="#1565C0"/>
    <SolidColorBrush x:Key="OnPrimaryBrush" Color="White"/>

    <!-- テキストカラー -->
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="Gray"/>
    <SolidColorBrush x:Key="MutedTextBrush" Color="#666666"/>
    <SolidColorBrush x:Key="DangerTextBrush" Color="#D32F2F"/>
    <!-- ヒント／補足説明用（Brown 700）。色覚多様性に配慮した茶色系で、💡や⚠の説明文に使う（Issue #1461） -->
    <SolidColorBrush x:Key="HintForegroundBrush" Color="#795548"/>

    <!-- 背景・ボーダー -->
    <SolidColorBrush x:Key="NeutralBackgroundBrush" Color="#F5F5F5"/>
    <SolidColorBrush x:Key="NeutralBorderBrush" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="DividerBrush" Color="#BDBDBD"/>
    <SolidColorBrush x:Key="SubtleBorderBrush" Color="#CCCCCC"/>
    <SolidColorBrush x:Key="AlternatingRowBrush" Color="#FAFAFA"/>

    <!-- アクション・オーバーレイ -->
    <SolidColorBrush x:Key="SuccessActionBrush" Color="#4CAF50"/>
    <SolidColorBrush x:Key="WarningActionBrush" Color="#FF9800"/>
    <SolidColorBrush x:Key="OverlayBrush" Color="#80000000"/>

    <!-- ===== 明細グループ配色（履歴詳細ダイアログ） ===== -->
    <!-- Issue #1822: LedgerDetailDialog.xaml 内で色値リテラルを直書きしていたものを SSOT へ集約。
         状態ブラシ（Lending/Return/Error 等）とは意味が異なるため流用せず専用キーとして定義する。
         GroupColorIndex 1-5（グループ番号）に対応する 5 色サイクル。
         0（自動検出）はスタイル既定値の Transparent が使われるためキーを持たない
         （移設前の GroupColor0 はどこからも参照されていなかったため引き継がない）。 -->
    <!-- 行背景（淡色） -->
    <SolidColorBrush x:Key="LedgerGroupBackground1Brush" Color="#E3F2FD"/>
    <SolidColorBrush x:Key="LedgerGroupBackground2Brush" Color="#E8F5E9"/>
    <SolidColorBrush x:Key="LedgerGroupBackground3Brush" Color="#FFF3E0"/>
    <SolidColorBrush x:Key="LedgerGroupBackground4Brush" Color="#F3E5F5"/>
    <SolidColorBrush x:Key="LedgerGroupBackground5Brush" Color="#FFEBEE"/>
    <!-- グループバッジ（同サイクルの濃色） -->
    <SolidColorBrush x:Key="LedgerGroupBadge1Brush" Color="#1976D2"/>
    <SolidColorBrush x:Key="LedgerGroupBadge2Brush" Color="#388E3C"/>
    <SolidColorBrush x:Key="LedgerGroupBadge3Brush" Color="#F57C00"/>
    <SolidColorBrush x:Key="LedgerGroupBadge4Brush" Color="#7B1FA2"/>
    <SolidColorBrush x:Key="LedgerGroupBadge5Brush" Color="#D32F2F"/>

    <!-- ===== グラフ系列配色（管理者ダッシュボード F8） ===== -->
    <!-- Issue #1855: 系列色に業務画面の意味色（Primary=青／SuccessAction=緑／WarningAction=橙／
         DangerText=赤／InfoText=青）を流用していたため、1 番目 #1976D2 と 5 番目 #1565C0 の
         CIE L*a*b* 距離が ΔE=7.1 しかなく肉眼では同色だった。意味色は「成功／警告／危険」という
         意味を担うため色数を増やせず、系列色として使う限り同じ衝突が再発する。
         系列色は意味色から分離した専用キーとして定義する。
         配色は Okabe-Ito（色覚多様性に配慮した定性パレット）を採用。実測値は次のとおり:
           ChartSeries1 #0072B2 相対輝度 0.152 / ChartSeries2 #D55E00 0.222 /
           ChartSeries3 #009E73 0.257 / ChartSeries4 #E69F00 0.416 /
           ChartSeries5 #CC79A7 0.293 / ChartSeriesOther #424242 0.054
         6 色間の最小 ΔE=33.4、最小 相対輝度差=0.035、P/D/T 型色覚シミュレーション後の
         最小 ΔE=16.5。しきい値は ChartSeriesPaletteTests が固定する。
         ChartSeries4（橙）の白背景コントラストは 2.25 で WCAG 1.4.11 の 3:1 を下回るが、
         暗くすると赤緑色覚で ChartSeries2（朱色）と混同域に入る（ΔEcvd 16.5→1.3 実測）ため
         Okabe-Ito の値を維持し、境界は塗りではなく ChartSeriesOutlineBrush の輪郭線で担保する。 -->
    <SolidColorBrush x:Key="ChartSeries1Brush" Color="#0072B2"/>
    <SolidColorBrush x:Key="ChartSeries2Brush" Color="#D55E00"/>
    <SolidColorBrush x:Key="ChartSeries3Brush" Color="#009E73"/>
    <SolidColorBrush x:Key="ChartSeries4Brush" Color="#E69F00"/>
    <SolidColorBrush x:Key="ChartSeries5Brush" Color="#CC79A7"/>
    <!-- 「その他」（上位 5 系列に入らない残りの集約）。集約された残りであることが色からも
         伝わるよう無彩色を充てる。旧 MutedTextBrush(#666666) は、当時 5 番目の系列だった
         InfoTextBrush(#1565C0) と相対輝度がほぼ同一（0.133 / 0.133）で、
         グレースケール印刷・ロービジョンで分離できなかった -->
    <SolidColorBrush x:Key="ChartSeriesOtherBrush" Color="#424242"/>
    <!-- 積み上げ棒の区画境界と凡例スウォッチの輪郭線。色以外の手掛かりを 1 つ足す（Issue #1855）。
         区切り線が満たすべき条件は 2 つあり、片方だけでは足りない:
           ①隣接する 2 つの塗りから見分けられること
           ②プロット領域の地色（白）から見分けられること
         白（#FFFFFF）は ① では最良（塗りとの最小コントラスト 2.25）だが ② が 1.00 で、
         ChartGeometryCalculator.CalculateStackedVerticalBars は最小高さを設けないため、
         月合計の 0.5% 未満しか占めない系列の区画（1px 未満）が線に覆われて完全に消える。
         「値が小さい」ではなく「データが無い」と読めてしまい、本 Issue の目的と逆になる。
         黒は ① が 2.09 と白とほぼ同等のまま ② が 21.0 で、極薄の区画も細い線として残り、
         棒そのものの輪郭も地色から立つ。判定は ChartSeriesPaletteTests が色値で固定する。 -->
    <SolidColorBrush x:Key="ChartSeriesOutlineBrush" Color="#000000"/>

    <!-- ===== アニメーション用カラー ===== -->
    <!-- ColorAnimation は Brush ではなく Color を直接補間するため、SolidColorBrush ではなく Color で定義する。 -->
    <!-- 行ハイライト（薄い黄色）: 新規登録・更新・復元後に該当行を一瞬ハイライトしてフェードアウト（Issue #1613） -->
    <Color x:Key="RowHighlightColor">#FFF9C4</Color>

    <!-- ===== フォーカス表示スタイル ===== -->
    <!-- キーボードナビゲーション用の明確なフォーカス表示 -->

    <Style x:Key="AccessibleFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border BorderBrush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"
                            BorderThickness="2"
                            CornerRadius="2"
                            Margin="-2">
                        <Border BorderBrush="White"
                                BorderThickness="1"
                                CornerRadius="1"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== ボタンスタイル ===== -->
    <!-- 高齢者向けに最適化されたボタンサイズとフォーカス表示 -->
    <!-- 高コントラストモード時はシステムカラーを自動適用 -->
    <Style x:Key="AccessibleButtonStyle" TargetType="Button">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Setter Property="Padding" Value="15,8"/>
        <Setter Property="MinWidth" Value="75"/>
        <Setter Property="MinHeight" Value="40"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <!-- 高コントラストモード対応 -->
                        <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- プライマリボタンスタイル -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccessibleButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- 危険ボタンスタイル（削除など） -->
    <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource AccessibleButtonStyle}">
        <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ErrorForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBorderBrush}"/>
    </Style>

    <!-- ===== テキストボックススタイル ===== -->
    <Style x:Key="AccessibleTextBoxStyle" TargetType="TextBox">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource NeutralBackgroundBrush}"/>
                            <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== コンボボックススタイル ===== -->
    <Style x:Key="AccessibleComboBoxStyle" TargetType="ComboBox">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="MinHeight" Value="32"/>
        <Style.Triggers>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===== データグリッドスタイル ===== -->
    <Style x:Key="AccessibleDataGridStyle" TargetType="DataGrid">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource AlternatingRowBrush}"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource NeutralBorderBrush}"/>
    </Style>

    <Style x:Key="AccessibleDataGridRowStyle" TargetType="DataGridRow">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===== リストビュースタイル ===== -->
    <Style x:Key="AccessibleListViewItemStyle" TargetType="ListViewItem">
        <Setter Property="FocusVisualStyle" Value="{StaticResource AccessibleFocusVisual}"/>
        <Setter Property="Padding" Value="8"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===== ステータス表示スタイル ===== -->
    <!-- 色とアイコンの両方で状態を示す -->

    <Style x:Key="StatusIndicatorStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <Style x:Key="LendingStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="{StaticResource LendingBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource LendingBorderBrush}"/>
    </Style>

    <Style x:Key="ReturnStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="{StaticResource ReturnBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ReturnBorderBrush}"/>
    </Style>

    <Style x:Key="ErrorStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="{StaticResource ErrorBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource ErrorBorderBrush}"/>
    </Style>

    <Style x:Key="SuccessStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="{StaticResource SuccessBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource SuccessBorderBrush}"/>
    </Style>

    <Style x:Key="WarningStatusStyle" TargetType="Border" BasedOn="{StaticResource StatusIndicatorStyle}">
        <Setter Property="Background" Value="{StaticResource WarningBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WarningBorderBrush}"/>
    </Style>

    <!-- ===== ハイコントラストモード対応 ===== -->
    <!--
         高コントラストモードへの対応方法:

         1. AccessibleButtonStyle等の主要スタイルには高コントラスト対応が組み込まれています
            → Style="{StaticResource AccessibleButtonStyle}" を使用するだけで自動対応

         2. 個別に高コントラスト対応が必要な場合は以下のユーティリティスタイルを使用:
            - HighContrastTextStyle: TextBlock用
            - HighContrastBorderStyle: Border用
            - HighContrastButtonStyle: 追加のボタンカスタマイズ用

         使用例:
         <TextBlock Style="{StaticResource HighContrastTextStyle}" Text="..." />
         <Border Style="{StaticResource HighContrastBorderStyle}">...</Border>
    -->

    <!-- 高コントラスト対応TextBlockスタイル（個別適用用） -->
    <Style x:Key="HighContrastTextStyle" TargetType="TextBlock">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- 高コントラスト対応Borderスタイル（個別適用用） -->
    <Style x:Key="HighContrastBorderStyle" TargetType="Border">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- 高コントラスト対応Buttonスタイル（個別適用用、AccessibleButtonStyleには既に組み込み済み） -->
    <Style x:Key="HighContrastButtonStyle" TargetType="Button">
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!-- ===== ツールチップスタイル ===== -->
    <Style x:Key="AccessibleToolTipStyle" TargetType="ToolTip">
        <Setter Property="Background" Value="{StaticResource WaitingForegroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource OnPrimaryBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontSize" Value="{DynamicResource BaseFontSize}"/>
        <Setter Property="HasDropShadow" Value="True"/>
    </Style>

    <!-- ===== スクリーンリーダー用ラベルスタイル ===== -->
    <!-- 視覚的には見えないが、スクリーンリーダーには読み上げられる -->
    <Style x:Key="ScreenReaderOnlyStyle" TargetType="TextBlock">
        <Setter Property="Width" Value="1"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="-1"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Visibility" Value="Visible"/>
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
    </Style>

    <!-- ===== バリデーションエラー表示スタイル（Issue #1279） ===== -->
    <!--
         Validation.ErrorTemplate を定義し、TextBox のバインディング検証が失敗した際に
         赤枠 + 右側エラーメッセージを表示する。色覚多様性対応のため、色のみでなく
         太い枠線とエラーアイコン（⚠）を併用する。
    -->
    <ControlTemplate x:Key="ValidationErrorTemplate">
        <StackPanel Orientation="Horizontal">
            <Border BorderBrush="{StaticResource ErrorBorderBrush}"
                    BorderThickness="2"
                    CornerRadius="4">
                <AdornedElementPlaceholder x:Name="AdornedElement"/>
            </Border>
            <TextBlock Text="⚠"
                       Foreground="{StaticResource ErrorForegroundBrush}"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="6,0,0,0"
                       ToolTip="{Binding [0].ErrorContent}"
                       AutomationProperties.Name="入力エラーあり"
                       AutomationProperties.HelpText="この入力欄に検証エラーがあります。詳細はツールチップまたは画面下部のエラー表示を参照してください。"/>
        </StackPanel>
    </ControlTemplate>

    <!-- 暗黙的に全 TextBox に適用されるスタイル。Issue #1279 によりエラー時の視覚変化を追加 -->
    <!-- このスタイルは x:Key を持たないため、ResourceDictionary 配下の全 TextBox に自動適用される -->
    <Style TargetType="TextBox">
        <Setter Property="Validation.ErrorTemplate" Value="{StaticResource ValidationErrorTemplate}"/>
        <Style.Triggers>
            <Trigger Property="Validation.HasError" Value="True">
                <Setter Property="ToolTip"
                        Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ErrorBorderBrush}"/>
                <Setter Property="BorderThickness" Value="2"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
