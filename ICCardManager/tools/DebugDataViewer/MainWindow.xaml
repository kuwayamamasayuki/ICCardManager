<Window x:Class="DebugDataViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DebugDataViewer"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        Title="ICカード管理システム - デバッグ用データビューア"
        Height="850"
        Width="1000"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- モノスペースフォントスタイル -->
        <Style x:Key="MonospaceTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="MonospaceTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="#F5F5F5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ヘッダー -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="デバッグ用データビューア" FontSize="20" FontWeight="Bold"/>
            <TextBlock Text=" - ICカードの生データおよびDBデータを確認"
                       FontSize="14"
                       Foreground="Gray"
                       VerticalAlignment="Bottom"
                       Margin="10,0,0,2"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <!-- カードデータタブ -->
            <TabItem Header="カードデータ">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- カード読み取りボタン -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="カードを読み取る"
                                Command="{Binding ReadCardCommand}"
                                Padding="15,10"
                                Background="#2196F3"
                                Foreground="White"
                                IsEnabled="{Binding IsNotWaitingForCard}"
                                ToolTip="カードリーダーにカードをタッチしてデータを読み取ります"/>
                        <TextBlock Text="{Binding CardStatusMessage}"
                                   VerticalAlignment="Center"
                                   Margin="15,0,0,0"
                                   FontWeight="Bold"/>
                    </StackPanel>

                    <!-- 基本情報 -->
                    <GroupBox Grid.Row="1" Header="基本情報" Padding="10" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="IDm:" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding CardIdm, Mode=OneWay}"
                                     Style="{StaticResource MonospaceTextBoxStyle}" Margin="0,2"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="システムコード:" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding CardSystemCode, Mode=OneWay}"
                                     Style="{StaticResource MonospaceTextBoxStyle}" Margin="0,2"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="残高:" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding CardBalance, Mode=OneWay}"
                                     Style="{StaticResource MonospaceTextBoxStyle}" Margin="0,2"/>
                        </Grid>
                    </GroupBox>

                    <!-- 生データ表示 -->
                    <GroupBox Grid.Row="2" Header="生データ（16進数）" Padding="10" Margin="0,0,0,10">
                        <TextBox Text="{Binding RawHistoryData, Mode=OneWay}"
                                 Style="{StaticResource MonospaceTextBoxStyle}"
                                 Height="100"
                                 TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"/>
                    </GroupBox>

                    <!-- 履歴データ（成形済み）+ ビット解析 -->
                    <GroupBox Grid.Row="3" Header="履歴データ（成形済み）" Padding="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 履歴データグリッド -->
                            <DataGrid Grid.Row="0"
                                      ItemsSource="{Binding CardHistoryItems}"
                                      SelectedItem="{Binding SelectedCardHistoryItem, Mode=TwoWay}"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      GridLinesVisibility="Horizontal"
                                      HeadersVisibility="Column"
                                      VerticalScrollBarVisibility="Auto">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="30"/>
                                    <DataGridTextColumn Header="利用日時" Binding="{Binding UseDate, StringFormat=yyyy/MM/dd HH:mm}" Width="120"/>
                                    <DataGridTextColumn Header="乗車駅" Binding="{Binding EntryStation}" Width="100"/>
                                    <DataGridTextColumn Header="降車駅" Binding="{Binding ExitStation}" Width="100"/>
                                    <DataGridTextColumn Header="金額" Binding="{Binding Amount, StringFormat=¥{0:N0}}" Width="70"/>
                                    <DataGridTextColumn Header="残高" Binding="{Binding Balance, StringFormat=¥{0:N0}}" Width="70"/>
                                    <DataGridTextColumn Header="種別" Binding="{Binding TransactionType}" Width="60"/>
                                    <DataGridTextColumn Header="生データ" Binding="{Binding RawData}" Width="*">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="FontFamily" Value="Consolas, Courier New, monospace"/>
                                                <Setter Property="FontSize" Value="10"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                            <!-- リサイズ用スプリッター -->
                            <GridSplitter Grid.Row="1"
                                          Height="5"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"
                                          Background="#CCCCCC"/>

                            <!-- ビット単位フィールド解析パネル -->
                            <GroupBox Grid.Row="2"
                                      Header="ビット単位フィールド解析（選択行）"
                                      Padding="5"
                                      Margin="0,5,0,0">
                                <TextBox Text="{Binding SelectedItemBitDetail, Mode=OneWay}"
                                         Style="{StaticResource MonospaceTextBoxStyle}"
                                         FontSize="11"
                                         TextWrapping="NoWrap"
                                         VerticalScrollBarVisibility="Auto"
                                         HorizontalScrollBarVisibility="Auto"
                                         VerticalAlignment="Stretch"/>
                            </GroupBox>
                        </Grid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- DBデータタブ -->
            <TabItem Header="DBデータ">
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- テーブル選択 -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="テーブル:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <ComboBox ItemsSource="{Binding TableNames}"
                                  SelectedItem="{Binding SelectedTableName}"
                                  Width="200"
                                  VerticalAlignment="Center"/>
                        <Button Content="データを読み込む"
                                Command="{Binding LoadTableDataCommand}"
                                Padding="15,10"
                                Margin="15,0,0,0"
                                Background="#4CAF50"
                                Foreground="White"/>
                        <TextBlock Text="{Binding DbStatusMessage}"
                                   VerticalAlignment="Center"
                                   Margin="15,0,0,0"/>
                    </StackPanel>

                    <!-- データグリッド -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding TableData}"
                              AutoGenerateColumns="True"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              GridLinesVisibility="All"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Auto">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                    </DataGrid>

                    <!-- レコード数 -->
                    <TextBlock Grid.Row="2"
                               Text="{Binding RecordCount, StringFormat=レコード数: {0}件}"
                               Margin="0,10,0,0"/>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- フッター -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <TextBlock Text="{Binding DatabasePath, StringFormat=DB: {0}}"
                       Foreground="Gray"
                       FontSize="11"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <Button Content="選択..."
                    Command="{Binding SelectDatabaseCommand}"
                    Padding="10,5"
                    FontSize="11"
                    VerticalAlignment="Center"
                    ToolTip="別のデータベースファイルを開く"
                    Margin="0,0,20,0"/>
            <Button Content="閉じる"
                    Click="CloseButton_Click"
                    Padding="20,10"
                    MinWidth="100"/>
        </StackPanel>

        <!-- 処理中オーバーレイ -->
        <Border Grid.RowSpan="3"
                Background="#80000000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="{Binding BusyMessage}"
                           Foreground="White"
                           FontSize="14"
                           Margin="0,10,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
